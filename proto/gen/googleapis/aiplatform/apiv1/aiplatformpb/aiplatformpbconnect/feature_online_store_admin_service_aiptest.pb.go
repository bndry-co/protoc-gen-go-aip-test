// Code generated by protoc-gen-go-aip-test. DO NOT EDIT.

package aiplatformpbconnect

import (
	aiplatformpb "cloud.google.com/go/aiplatform/apiv1/aiplatformpb"
	connect "connectrpc.com/connect"
	context "context"
	cmpopts "github.com/google/go-cmp/cmp/cmpopts"
	proto "google.golang.org/protobuf/proto"
	protocmp "google.golang.org/protobuf/testing/protocmp"
	fieldmaskpb "google.golang.org/protobuf/types/known/fieldmaskpb"
	assert "gotest.tools/v3/assert"
	strings "strings"
	testing "testing"
)

// FeatureOnlineStoreAdminServiceTestSuiteConfigProvider is the interface to implement to decide which resources
// that should be tested and how it's configured.
type FeatureOnlineStoreAdminServiceTestSuiteConfigProvider interface {
	// FeatureOnlineStoreAdminServiceFeatureOnlineStore should return a config, or nil, which means that the tests will be skipped.
	FeatureOnlineStoreAdminServiceFeatureOnlineStore(t *testing.T) *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig
	// FeatureOnlineStoreAdminServiceFeatureView should return a config, or nil, which means that the tests will be skipped.
	FeatureOnlineStoreAdminServiceFeatureView(t *testing.T) *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig
	// FeatureOnlineStoreAdminServiceFeatureViewSync should return a config, or nil, which means that the tests will be skipped.
	FeatureOnlineStoreAdminServiceFeatureViewSync(t *testing.T) *FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig
}

// testFeatureOnlineStoreAdminService is the main entrypoint for starting the AIP tests.
func testFeatureOnlineStoreAdminService(t *testing.T, s FeatureOnlineStoreAdminServiceTestSuiteConfigProvider) {
	testFeatureOnlineStoreAdminServiceFeatureOnlineStore(t, s)
	testFeatureOnlineStoreAdminServiceFeatureView(t, s)
	testFeatureOnlineStoreAdminServiceFeatureViewSync(t, s)
}

func testFeatureOnlineStoreAdminServiceFeatureOnlineStore(t *testing.T, s FeatureOnlineStoreAdminServiceTestSuiteConfigProvider) {
	t.Run("FeatureOnlineStore", func(t *testing.T) {
		config := s.FeatureOnlineStoreAdminServiceFeatureOnlineStore(t)
		if config == nil {
			t.Skip("Method FeatureOnlineStoreAdminServiceFeatureOnlineStore not implemented")
		}
		if config.Service == nil {
			t.Skip("Method FeatureOnlineStoreAdminServiceFeatureOnlineStore.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testFeatureOnlineStoreAdminServiceFeatureView(t *testing.T, s FeatureOnlineStoreAdminServiceTestSuiteConfigProvider) {
	t.Run("FeatureView", func(t *testing.T) {
		config := s.FeatureOnlineStoreAdminServiceFeatureView(t)
		if config == nil {
			t.Skip("Method FeatureOnlineStoreAdminServiceFeatureView not implemented")
		}
		if config.Service == nil {
			t.Skip("Method FeatureOnlineStoreAdminServiceFeatureView.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testFeatureOnlineStoreAdminServiceFeatureViewSync(t *testing.T, s FeatureOnlineStoreAdminServiceTestSuiteConfigProvider) {
	t.Run("FeatureViewSync", func(t *testing.T) {
		config := s.FeatureOnlineStoreAdminServiceFeatureViewSync(t)
		if config == nil {
			t.Skip("Method FeatureOnlineStoreAdminServiceFeatureViewSync not implemented")
		}
		if config.Service == nil {
			t.Skip("Method FeatureOnlineStoreAdminServiceFeatureViewSync.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

type FeatureOnlineStoreAdminServiceTestSuite struct {
	T *testing.T
	// Client to test.
	Client FeatureOnlineStoreAdminServiceClient
}

func (fx FeatureOnlineStoreAdminServiceTestSuite) TestFeatureOnlineStore(ctx context.Context, options FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) {
	fx.T.Run("FeatureOnlineStore", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() FeatureOnlineStoreAdminServiceClient { return fx.Client }
		options.test(t)
	})
}

func (fx FeatureOnlineStoreAdminServiceTestSuite) TestFeatureView(ctx context.Context, options FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) {
	fx.T.Run("FeatureView", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() FeatureOnlineStoreAdminServiceClient { return fx.Client }
		options.test(t)
	})
}

func (fx FeatureOnlineStoreAdminServiceTestSuite) TestFeatureViewSync(ctx context.Context, options FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig) {
	fx.T.Run("FeatureViewSync", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() FeatureOnlineStoreAdminServiceClient { return fx.Client }
		options.test(t)
	})
}

type FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() FeatureOnlineStoreAdminServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *aiplatformpb.FeatureOnlineStore
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *aiplatformpb.FeatureOnlineStore
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureOnlineStoreRequest{
			Parent:             "",
			FeatureOnlineStore: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureOnlineStoreRequest{
			Parent:             "invalid resource name",
			FeatureOnlineStore: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".bigtable.auto_scaling", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetBigtable()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("auto_scaling")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureOnlineStoreRequest{
				Parent:             parent,
				FeatureOnlineStore: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".bigtable.auto_scaling.min_node_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetBigtable().GetAutoScaling()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("min_node_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureOnlineStoreRequest{
				Parent:             parent,
				FeatureOnlineStore: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".bigtable.auto_scaling.max_node_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetBigtable().GetAutoScaling()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("max_node_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureOnlineStoreRequest{
				Parent:             parent,
				FeatureOnlineStore: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureOnlineStoreRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureOnlineStoreRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureOnlineStoreRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureOnlineStoreRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureOnlineStoreRequest{
			Name: "projects/-/locations/-/featureOnlineStores/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.Name = ""
		_, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
			FeatureOnlineStore: msg,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.Name = "invalid resource name"
		_, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
			FeatureOnlineStore: msg,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.Name = created.Name
		_, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
			FeatureOnlineStore: msg,
		}))
		assert.Equal(t, connect.CodeAborted, connect.CodeOf(err), err)
	})

	// Field etag should have a new value when the resource is successfully updated.
	t.Run("etag updated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.Name = created.Name
		updateResp, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
			FeatureOnlineStore: msg,
		}))
		updated := updateResp.Msg
		assert.NilError(t, err)
		_ = updated
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.Name = created.Name + "notfound"
			_, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
				FeatureOnlineStore: msg,
			}))
			assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			_, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
				FeatureOnlineStore: created,
				UpdateMask: &fieldmaskpb.FieldMask{
					Paths: []string{
						"invalid_field_xyz",
					},
				},
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".bigtable.auto_scaling", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.FeatureOnlineStore)
				container := msg.GetBigtable()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("auto_scaling")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
					FeatureOnlineStore: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".bigtable.auto_scaling.min_node_count", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.FeatureOnlineStore)
				container := msg.GetBigtable().GetAutoScaling()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("min_node_count")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
					FeatureOnlineStore: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".bigtable.auto_scaling.max_node_count", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.FeatureOnlineStore)
				container := msg.GetBigtable().GetAutoScaling()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("max_node_count")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureOnlineStoreRequest{
					FeatureOnlineStore: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
		})

	}
}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.FeatureOnlineStore, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.FeatureOnlineStores,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureOnlineStore) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.FeatureOnlineStore, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.FeatureOnlineStores))
				msgs = append(msgs, response.FeatureOnlineStores...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureOnlineStore) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				_, err := fx.Service().DeleteFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureOnlineStoreRequest{
					Name: parentMsgs[i].Name,
				}))
				assert.NilError(t, err)
			}
			listResp, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
				Parent:   parent,
				PageSize: 9999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.FeatureOnlineStores,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureOnlineStore) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.FeatureOnlineStore, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.FeatureOnlineStore, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListFeatureOnlineStores(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureOnlineStoresRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.FeatureOnlineStores...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureOnlineStore) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureOnlineStoreRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureOnlineStoreRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureOnlineStoreRequest{
			Name: created.Name,
		}))
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureOnlineStoreRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		deleteResp, err := fx.Service().DeleteFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureOnlineStoreRequest{
			Name: created.Name,
		}))
		deleted := deleteResp.Msg
		assert.NilError(t, err)
		_ = deleted
		_, err = fx.Service().DeleteFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureOnlineStoreRequest{
			Name: created.Name,
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteFeatureOnlineStore(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureOnlineStoreRequest{
			Name: "projects/-/locations/-/featureOnlineStores/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *FeatureOnlineStoreAdminServiceFeatureOnlineStoreTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.FeatureOnlineStore {
	t.Helper()
	t.Skip("Long running create method not supported")
	return nil
}

type FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() FeatureOnlineStoreAdminServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *aiplatformpb.FeatureView
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *aiplatformpb.FeatureView
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureViewRequest{
			Parent:      "",
			FeatureView: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureViewRequest{
			Parent:      "invalid resource name",
			FeatureView: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".big_query_source.uri", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetBigQuerySource()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("uri")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureViewRequest{
				Parent:      parent,
				FeatureView: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".big_query_source.entity_id_columns", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetBigQuerySource()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("entity_id_columns")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureViewRequest{
				Parent:      parent,
				FeatureView: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".feature_registry_source.feature_groups", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetFeatureRegistrySource()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("feature_groups")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.CreateFeatureViewRequest{
				Parent:      parent,
				FeatureView: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewRequest{
			Name: "projects/-/locations/-/featureOnlineStores/-/featureViews/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.Name = ""
		_, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
			FeatureView: msg,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.Name = "invalid resource name"
		_, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
			FeatureView: msg,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.Name = created.Name
		_, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
			FeatureView: msg,
		}))
		assert.Equal(t, connect.CodeAborted, connect.CodeOf(err), err)
	})

	// Field etag should have a new value when the resource is successfully updated.
	t.Run("etag updated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.Name = created.Name
		updateResp, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
			FeatureView: msg,
		}))
		updated := updateResp.Msg
		assert.NilError(t, err)
		_ = updated
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.Name = created.Name + "notfound"
			_, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
				FeatureView: msg,
			}))
			assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			_, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
				FeatureView: created,
				UpdateMask: &fieldmaskpb.FieldMask{
					Paths: []string{
						"invalid_field_xyz",
					},
				},
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".big_query_source.uri", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.FeatureView)
				container := msg.GetBigQuerySource()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("uri")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
					FeatureView: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".big_query_source.entity_id_columns", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.FeatureView)
				container := msg.GetBigQuerySource()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("entity_id_columns")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
					FeatureView: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".feature_registry_source.feature_groups", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.FeatureView)
				container := msg.GetFeatureRegistrySource()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("feature_groups")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateFeatureViewRequest{
					FeatureView: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
		})

	}
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.FeatureView, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.FeatureViews,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureView) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.FeatureView, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.FeatureViews))
				msgs = append(msgs, response.FeatureViews...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureView) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				_, err := fx.Service().DeleteFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureViewRequest{
					Name: parentMsgs[i].Name,
				}))
				assert.NilError(t, err)
			}
			listResp, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
				Parent:   parent,
				PageSize: 9999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.FeatureViews,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureView) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.FeatureView, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.FeatureView, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListFeatureViews(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.FeatureViews...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureView) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureViewRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureViewRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureViewRequest{
			Name: created.Name,
		}))
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureViewRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		deleteResp, err := fx.Service().DeleteFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureViewRequest{
			Name: created.Name,
		}))
		deleted := deleteResp.Msg
		assert.NilError(t, err)
		_ = deleted
		_, err = fx.Service().DeleteFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureViewRequest{
			Name: created.Name,
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteFeatureView(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteFeatureViewRequest{
			Name: "projects/-/locations/-/featureOnlineStores/-/featureViews/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.FeatureView {
	t.Helper()
	t.Skip("Long running create method not supported")
	return nil
}

type FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() FeatureOnlineStoreAdminServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// CreateResource should create a FeatureViewSync and return it.
	// If the field is not set, some tests will be skipped.
	//
	// This method is generated because service does not expose a Create
	// method (or it does not comply with AIP).
	CreateResource func(ctx context.Context, parent string) (*aiplatformpb.FeatureViewSync, error)
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig) test(t *testing.T) {
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureViewSync(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewSyncRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureViewSync(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewSyncRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetFeatureViewSync(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewSyncRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetFeatureViewSync(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewSyncRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetFeatureViewSync(fx.Context(), connect.NewRequest(&aiplatformpb.GetFeatureViewSyncRequest{
			Name: "projects/-/locations/-/featureOnlineStores/-/featureViews/-/featureViewSyncs/feature_view_sync",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListFeatureViewSyncs(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewSyncsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListFeatureViewSyncs(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewSyncsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListFeatureViewSyncs(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewSyncsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.FeatureViewSync, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureViewSyncs(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewSyncsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.FeatureViewSyncs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureViewSync) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureViewSyncs(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewSyncsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListFeatureViewSyncs(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewSyncsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.FeatureViewSync, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListFeatureViewSyncs(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewSyncsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.FeatureViewSyncs))
				msgs = append(msgs, response.FeatureViewSyncs...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureViewSync) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.FeatureViewSync, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.FeatureViewSync, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListFeatureViewSyncs(fx.Context(), connect.NewRequest(&aiplatformpb.ListFeatureViewSyncsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.FeatureViewSyncs...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.FeatureViewSync) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.FeatureViewSync {
	t.Helper()
	if fx.CreateResource == nil {
		t.Skip("Test skipped because CreateResource not specified on FeatureOnlineStoreAdminServiceFeatureViewSyncTestSuiteConfig")
	}
	created, err := fx.CreateResource(fx.Context(), parent)
	assert.NilError(t, err)
	return created
}
