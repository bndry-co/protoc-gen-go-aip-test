// Code generated by protoc-gen-go-aip-test. DO NOT EDIT.

package aiplatformpbconnect

import (
	aiplatformpb "cloud.google.com/go/aiplatform/apiv1/aiplatformpb"
	connect "connectrpc.com/connect"
	context "context"
	cmpopts "github.com/google/go-cmp/cmp/cmpopts"
	proto "google.golang.org/protobuf/proto"
	protocmp "google.golang.org/protobuf/testing/protocmp"
	fieldmaskpb "google.golang.org/protobuf/types/known/fieldmaskpb"
	assert "gotest.tools/v3/assert"
	strings "strings"
	testing "testing"
	time "time"
)

// JobServiceTestSuiteConfigProvider is the interface to implement to decide which resources
// that should be tested and how it's configured.
type JobServiceTestSuiteConfigProvider interface {
	// JobServiceBatchPredictionJob should return a config, or nil, which means that the tests will be skipped.
	JobServiceBatchPredictionJob(t *testing.T) *JobServiceBatchPredictionJobTestSuiteConfig
	// JobServiceCustomJob should return a config, or nil, which means that the tests will be skipped.
	JobServiceCustomJob(t *testing.T) *JobServiceCustomJobTestSuiteConfig
	// JobServiceDataLabelingJob should return a config, or nil, which means that the tests will be skipped.
	JobServiceDataLabelingJob(t *testing.T) *JobServiceDataLabelingJobTestSuiteConfig
	// JobServiceHyperparameterTuningJob should return a config, or nil, which means that the tests will be skipped.
	JobServiceHyperparameterTuningJob(t *testing.T) *JobServiceHyperparameterTuningJobTestSuiteConfig
	// JobServiceModelDeploymentMonitoringJob should return a config, or nil, which means that the tests will be skipped.
	JobServiceModelDeploymentMonitoringJob(t *testing.T) *JobServiceModelDeploymentMonitoringJobTestSuiteConfig
	// JobServiceNasJob should return a config, or nil, which means that the tests will be skipped.
	JobServiceNasJob(t *testing.T) *JobServiceNasJobTestSuiteConfig
	// JobServiceNasTrialDetail should return a config, or nil, which means that the tests will be skipped.
	JobServiceNasTrialDetail(t *testing.T) *JobServiceNasTrialDetailTestSuiteConfig
}

// testJobService is the main entrypoint for starting the AIP tests.
func testJobService(t *testing.T, s JobServiceTestSuiteConfigProvider) {
	testJobServiceBatchPredictionJob(t, s)
	testJobServiceCustomJob(t, s)
	testJobServiceDataLabelingJob(t, s)
	testJobServiceHyperparameterTuningJob(t, s)
	testJobServiceModelDeploymentMonitoringJob(t, s)
	testJobServiceNasJob(t, s)
	testJobServiceNasTrialDetail(t, s)
}

func testJobServiceBatchPredictionJob(t *testing.T, s JobServiceTestSuiteConfigProvider) {
	t.Run("BatchPredictionJob", func(t *testing.T) {
		config := s.JobServiceBatchPredictionJob(t)
		if config == nil {
			t.Skip("Method JobServiceBatchPredictionJob not implemented")
		}
		if config.Service == nil {
			t.Skip("Method JobServiceBatchPredictionJob.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testJobServiceCustomJob(t *testing.T, s JobServiceTestSuiteConfigProvider) {
	t.Run("CustomJob", func(t *testing.T) {
		config := s.JobServiceCustomJob(t)
		if config == nil {
			t.Skip("Method JobServiceCustomJob not implemented")
		}
		if config.Service == nil {
			t.Skip("Method JobServiceCustomJob.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testJobServiceDataLabelingJob(t *testing.T, s JobServiceTestSuiteConfigProvider) {
	t.Run("DataLabelingJob", func(t *testing.T) {
		config := s.JobServiceDataLabelingJob(t)
		if config == nil {
			t.Skip("Method JobServiceDataLabelingJob not implemented")
		}
		if config.Service == nil {
			t.Skip("Method JobServiceDataLabelingJob.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testJobServiceHyperparameterTuningJob(t *testing.T, s JobServiceTestSuiteConfigProvider) {
	t.Run("HyperparameterTuningJob", func(t *testing.T) {
		config := s.JobServiceHyperparameterTuningJob(t)
		if config == nil {
			t.Skip("Method JobServiceHyperparameterTuningJob not implemented")
		}
		if config.Service == nil {
			t.Skip("Method JobServiceHyperparameterTuningJob.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testJobServiceModelDeploymentMonitoringJob(t *testing.T, s JobServiceTestSuiteConfigProvider) {
	t.Run("ModelDeploymentMonitoringJob", func(t *testing.T) {
		config := s.JobServiceModelDeploymentMonitoringJob(t)
		if config == nil {
			t.Skip("Method JobServiceModelDeploymentMonitoringJob not implemented")
		}
		if config.Service == nil {
			t.Skip("Method JobServiceModelDeploymentMonitoringJob.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testJobServiceNasJob(t *testing.T, s JobServiceTestSuiteConfigProvider) {
	t.Run("NasJob", func(t *testing.T) {
		config := s.JobServiceNasJob(t)
		if config == nil {
			t.Skip("Method JobServiceNasJob not implemented")
		}
		if config.Service == nil {
			t.Skip("Method JobServiceNasJob.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testJobServiceNasTrialDetail(t *testing.T, s JobServiceTestSuiteConfigProvider) {
	t.Run("NasTrialDetail", func(t *testing.T) {
		config := s.JobServiceNasTrialDetail(t)
		if config == nil {
			t.Skip("Method JobServiceNasTrialDetail not implemented")
		}
		if config.Service == nil {
			t.Skip("Method JobServiceNasTrialDetail.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

type JobServiceTestSuite struct {
	T *testing.T
	// Client to test.
	Client JobServiceClient
}

func (fx JobServiceTestSuite) TestBatchPredictionJob(ctx context.Context, options JobServiceBatchPredictionJobTestSuiteConfig) {
	fx.T.Run("BatchPredictionJob", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() JobServiceClient { return fx.Client }
		options.test(t)
	})
}

func (fx JobServiceTestSuite) TestCustomJob(ctx context.Context, options JobServiceCustomJobTestSuiteConfig) {
	fx.T.Run("CustomJob", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() JobServiceClient { return fx.Client }
		options.test(t)
	})
}

func (fx JobServiceTestSuite) TestDataLabelingJob(ctx context.Context, options JobServiceDataLabelingJobTestSuiteConfig) {
	fx.T.Run("DataLabelingJob", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() JobServiceClient { return fx.Client }
		options.test(t)
	})
}

func (fx JobServiceTestSuite) TestHyperparameterTuningJob(ctx context.Context, options JobServiceHyperparameterTuningJobTestSuiteConfig) {
	fx.T.Run("HyperparameterTuningJob", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() JobServiceClient { return fx.Client }
		options.test(t)
	})
}

func (fx JobServiceTestSuite) TestModelDeploymentMonitoringJob(ctx context.Context, options JobServiceModelDeploymentMonitoringJobTestSuiteConfig) {
	fx.T.Run("ModelDeploymentMonitoringJob", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() JobServiceClient { return fx.Client }
		options.test(t)
	})
}

func (fx JobServiceTestSuite) TestNasJob(ctx context.Context, options JobServiceNasJobTestSuiteConfig) {
	fx.T.Run("NasJob", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() JobServiceClient { return fx.Client }
		options.test(t)
	})
}

func (fx JobServiceTestSuite) TestNasTrialDetail(ctx context.Context, options JobServiceNasTrialDetailTestSuiteConfig) {
	fx.T.Run("NasTrialDetail", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() JobServiceClient { return fx.Client }
		options.test(t)
	})
}

type JobServiceBatchPredictionJobTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() JobServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *aiplatformpb.BatchPredictionJob
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
			Parent:             "",
			BatchPredictionJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
			Parent:             "invalid resource name",
			BatchPredictionJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		createResp, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
			Parent:             parent,
			BatchPredictionJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		assert.Check(t, msg.CreateTime != nil)
		assert.Check(t, !msg.CreateTime.AsTime().IsZero())
		assert.Check(t, msg.CreateTime.AsTime().After(beforeCreate), "msg.CreateTime (%v) is not after beforeCreate (%v)", msg.CreateTime.AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		createResp, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
			Parent:             parent,
			BatchPredictionJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		getResp, err := fx.Service().GetBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetBatchPredictionJobRequest{
			Name: msg.Name,
		}))
		persisted := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".unmanaged_container_model.container_spec.image_uri", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetUnmanagedContainerModel().GetContainerSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("image_uri")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".input_config", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("input_config")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".input_config.gcs_source.uris", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetInputConfig().GetGcsSource()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("uris")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".input_config.bigquery_source.input_uri", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetInputConfig().GetBigquerySource()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("input_uri")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".input_config.instances_format", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetInputConfig()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("instances_format")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".output_config", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("output_config")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".output_config.gcs_destination.output_uri_prefix", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetOutputConfig().GetGcsDestination()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("output_uri_prefix")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".output_config.bigquery_destination.output_uri", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetOutputConfig().GetBigqueryDestination()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("output_uri")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".output_config.predictions_format", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetOutputConfig()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("predictions_format")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".dedicated_resources.machine_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetDedicatedResources()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("machine_spec")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".explanation_spec.parameters", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetExplanationSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("parameters")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".explanation_spec.parameters.sampled_shapley_attribution.path_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetExplanationSpec().GetParameters().GetSampledShapleyAttribution()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("path_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".explanation_spec.parameters.integrated_gradients_attribution.step_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetExplanationSpec().GetParameters().GetIntegratedGradientsAttribution()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("step_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".explanation_spec.parameters.xrai_attribution.step_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetExplanationSpec().GetParameters().GetXraiAttribution()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("step_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".explanation_spec.metadata.inputs", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetExplanationSpec().GetMetadata()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("inputs")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".explanation_spec.metadata.outputs", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetExplanationSpec().GetMetadata()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("outputs")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetEncryptionSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".model", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			container.Model = "invalid resource name"
			_, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
				Parent:             parent,
				BatchPredictionJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetBatchPredictionJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetBatchPredictionJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetBatchPredictionJobRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetBatchPredictionJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetBatchPredictionJobRequest{
			Name: "projects/-/locations/-/batchPredictionJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.BatchPredictionJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.BatchPredictionJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.BatchPredictionJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.BatchPredictionJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.BatchPredictionJobs))
				msgs = append(msgs, response.BatchPredictionJobs...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.BatchPredictionJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				_, err := fx.Service().DeleteBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteBatchPredictionJobRequest{
					Name: parentMsgs[i].Name,
				}))
				assert.NilError(t, err)
			}
			listResp, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
				Parent:   parent,
				PageSize: 9999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.BatchPredictionJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.BatchPredictionJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.BatchPredictionJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.BatchPredictionJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListBatchPredictionJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListBatchPredictionJobsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.BatchPredictionJobs...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.BatchPredictionJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteBatchPredictionJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteBatchPredictionJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteBatchPredictionJobRequest{
			Name: created.Name,
		}))
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteBatchPredictionJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		deleteResp, err := fx.Service().DeleteBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteBatchPredictionJobRequest{
			Name: created.Name,
		}))
		deleted := deleteResp.Msg
		assert.NilError(t, err)
		_ = deleted
		_, err = fx.Service().DeleteBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteBatchPredictionJobRequest{
			Name: created.Name,
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteBatchPredictionJobRequest{
			Name: "projects/-/locations/-/batchPredictionJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *JobServiceBatchPredictionJobTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.BatchPredictionJob {
	t.Helper()
	createResp, err := fx.Service().CreateBatchPredictionJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateBatchPredictionJobRequest{
		Parent:             parent,
		BatchPredictionJob: fx.Create(parent),
	}))
	created := createResp.Msg
	assert.NilError(t, err)
	return created
}

type JobServiceCustomJobTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() JobServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *aiplatformpb.CustomJob
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *JobServiceCustomJobTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *JobServiceCustomJobTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
			Parent:    "",
			CustomJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
			Parent:    "invalid resource name",
			CustomJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		createResp, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
			Parent:    parent,
			CustomJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		assert.Check(t, msg.CreateTime != nil)
		assert.Check(t, !msg.CreateTime.AsTime().IsZero())
		assert.Check(t, msg.CreateTime.AsTime().After(beforeCreate), "msg.CreateTime (%v) is not after beforeCreate (%v)", msg.CreateTime.AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		createResp, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
			Parent:    parent,
			CustomJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		getResp, err := fx.Service().GetCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetCustomJobRequest{
			Name: msg.Name,
		}))
		persisted := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".job_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("job_spec")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".job_spec.worker_pool_specs", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("worker_pool_specs")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".job_spec.base_output_directory.output_uri_prefix", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetJobSpec().GetBaseOutputDirectory()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("output_uri_prefix")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetEncryptionSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".job_spec.network", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Network = "invalid resource name"
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".job_spec.tensorboard", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Tensorboard = "invalid resource name"
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".job_spec.experiment", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Experiment = "invalid resource name"
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".job_spec.experiment_run", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.ExperimentRun = "invalid resource name"
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".job_spec.models", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Models = []string{"invalid resource name"}
			_, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
				Parent:    parent,
				CustomJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

}

func (fx *JobServiceCustomJobTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetCustomJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetCustomJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetCustomJobRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetCustomJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetCustomJobRequest{
			Name: "projects/-/locations/-/customJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceCustomJobTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.CustomJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.CustomJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.CustomJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.CustomJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.CustomJobs))
				msgs = append(msgs, response.CustomJobs...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.CustomJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				_, err := fx.Service().DeleteCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteCustomJobRequest{
					Name: parentMsgs[i].Name,
				}))
				assert.NilError(t, err)
			}
			listResp, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
				Parent:   parent,
				PageSize: 9999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.CustomJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.CustomJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.CustomJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.CustomJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListCustomJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListCustomJobsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.CustomJobs...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.CustomJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *JobServiceCustomJobTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteCustomJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteCustomJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteCustomJobRequest{
			Name: created.Name,
		}))
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteCustomJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		deleteResp, err := fx.Service().DeleteCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteCustomJobRequest{
			Name: created.Name,
		}))
		deleted := deleteResp.Msg
		assert.NilError(t, err)
		_ = deleted
		_, err = fx.Service().DeleteCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteCustomJobRequest{
			Name: created.Name,
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteCustomJobRequest{
			Name: "projects/-/locations/-/customJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceCustomJobTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *JobServiceCustomJobTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *JobServiceCustomJobTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *JobServiceCustomJobTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.CustomJob {
	t.Helper()
	createResp, err := fx.Service().CreateCustomJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateCustomJobRequest{
		Parent:    parent,
		CustomJob: fx.Create(parent),
	}))
	created := createResp.Msg
	assert.NilError(t, err)
	return created
}

type JobServiceDataLabelingJobTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() JobServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *aiplatformpb.DataLabelingJob
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
			Parent:          "",
			DataLabelingJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
			Parent:          "invalid resource name",
			DataLabelingJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		createResp, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
			Parent:          parent,
			DataLabelingJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		assert.Check(t, msg.CreateTime != nil)
		assert.Check(t, !msg.CreateTime.AsTime().IsZero())
		assert.Check(t, msg.CreateTime.AsTime().After(beforeCreate), "msg.CreateTime (%v) is not after beforeCreate (%v)", msg.CreateTime.AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		createResp, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
			Parent:          parent,
			DataLabelingJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		getResp, err := fx.Service().GetDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetDataLabelingJobRequest{
			Name: msg.Name,
		}))
		persisted := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
				Parent:          parent,
				DataLabelingJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".datasets", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("datasets")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
				Parent:          parent,
				DataLabelingJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".labeler_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("labeler_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
				Parent:          parent,
				DataLabelingJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".instruction_uri", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("instruction_uri")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
				Parent:          parent,
				DataLabelingJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".inputs_schema_uri", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("inputs_schema_uri")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
				Parent:          parent,
				DataLabelingJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".inputs", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("inputs")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
				Parent:          parent,
				DataLabelingJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetEncryptionSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
				Parent:          parent,
				DataLabelingJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".datasets", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			container.Datasets = []string{"invalid resource name"}
			_, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
				Parent:          parent,
				DataLabelingJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetDataLabelingJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetDataLabelingJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetDataLabelingJobRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetDataLabelingJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetDataLabelingJobRequest{
			Name: "projects/-/locations/-/dataLabelingJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.DataLabelingJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.DataLabelingJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.DataLabelingJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.DataLabelingJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.DataLabelingJobs))
				msgs = append(msgs, response.DataLabelingJobs...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.DataLabelingJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				_, err := fx.Service().DeleteDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteDataLabelingJobRequest{
					Name: parentMsgs[i].Name,
				}))
				assert.NilError(t, err)
			}
			listResp, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
				Parent:   parent,
				PageSize: 9999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.DataLabelingJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.DataLabelingJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.DataLabelingJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.DataLabelingJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListDataLabelingJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListDataLabelingJobsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.DataLabelingJobs...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.DataLabelingJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteDataLabelingJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteDataLabelingJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteDataLabelingJobRequest{
			Name: created.Name,
		}))
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteDataLabelingJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		deleteResp, err := fx.Service().DeleteDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteDataLabelingJobRequest{
			Name: created.Name,
		}))
		deleted := deleteResp.Msg
		assert.NilError(t, err)
		_ = deleted
		_, err = fx.Service().DeleteDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteDataLabelingJobRequest{
			Name: created.Name,
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteDataLabelingJobRequest{
			Name: "projects/-/locations/-/dataLabelingJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *JobServiceDataLabelingJobTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.DataLabelingJob {
	t.Helper()
	createResp, err := fx.Service().CreateDataLabelingJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateDataLabelingJobRequest{
		Parent:          parent,
		DataLabelingJob: fx.Create(parent),
	}))
	created := createResp.Msg
	assert.NilError(t, err)
	return created
}

type JobServiceHyperparameterTuningJobTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() JobServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *aiplatformpb.HyperparameterTuningJob
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
			Parent:                  "",
			HyperparameterTuningJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
			Parent:                  "invalid resource name",
			HyperparameterTuningJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		createResp, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
			Parent:                  parent,
			HyperparameterTuningJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		assert.Check(t, msg.CreateTime != nil)
		assert.Check(t, !msg.CreateTime.AsTime().IsZero())
		assert.Check(t, msg.CreateTime.AsTime().After(beforeCreate), "msg.CreateTime (%v) is not after beforeCreate (%v)", msg.CreateTime.AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		createResp, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
			Parent:                  parent,
			HyperparameterTuningJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		getResp, err := fx.Service().GetHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetHyperparameterTuningJobRequest{
			Name: msg.Name,
		}))
		persisted := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".study_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("study_spec")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".study_spec.metrics", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetStudySpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("metrics")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".study_spec.parameters", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetStudySpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("parameters")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".max_trial_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("max_trial_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".parallel_trial_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("parallel_trial_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".trial_job_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("trial_job_spec")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".trial_job_spec.worker_pool_specs", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("worker_pool_specs")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".trial_job_spec.base_output_directory.output_uri_prefix", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetTrialJobSpec().GetBaseOutputDirectory()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("output_uri_prefix")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetEncryptionSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".trial_job_spec.network", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Network = "invalid resource name"
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".trial_job_spec.tensorboard", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Tensorboard = "invalid resource name"
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".trial_job_spec.experiment", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Experiment = "invalid resource name"
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".trial_job_spec.experiment_run", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.ExperimentRun = "invalid resource name"
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".trial_job_spec.models", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Models = []string{"invalid resource name"}
			_, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
				Parent:                  parent,
				HyperparameterTuningJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetHyperparameterTuningJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetHyperparameterTuningJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetHyperparameterTuningJobRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetHyperparameterTuningJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetHyperparameterTuningJobRequest{
			Name: "projects/-/locations/-/hyperparameterTuningJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.HyperparameterTuningJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.HyperparameterTuningJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.HyperparameterTuningJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.HyperparameterTuningJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.HyperparameterTuningJobs))
				msgs = append(msgs, response.HyperparameterTuningJobs...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.HyperparameterTuningJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				_, err := fx.Service().DeleteHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteHyperparameterTuningJobRequest{
					Name: parentMsgs[i].Name,
				}))
				assert.NilError(t, err)
			}
			listResp, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
				Parent:   parent,
				PageSize: 9999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.HyperparameterTuningJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.HyperparameterTuningJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.HyperparameterTuningJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.HyperparameterTuningJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListHyperparameterTuningJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListHyperparameterTuningJobsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.HyperparameterTuningJobs...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.HyperparameterTuningJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteHyperparameterTuningJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteHyperparameterTuningJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteHyperparameterTuningJobRequest{
			Name: created.Name,
		}))
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteHyperparameterTuningJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		deleteResp, err := fx.Service().DeleteHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteHyperparameterTuningJobRequest{
			Name: created.Name,
		}))
		deleted := deleteResp.Msg
		assert.NilError(t, err)
		_ = deleted
		_, err = fx.Service().DeleteHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteHyperparameterTuningJobRequest{
			Name: created.Name,
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteHyperparameterTuningJobRequest{
			Name: "projects/-/locations/-/hyperparameterTuningJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *JobServiceHyperparameterTuningJobTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.HyperparameterTuningJob {
	t.Helper()
	createResp, err := fx.Service().CreateHyperparameterTuningJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateHyperparameterTuningJobRequest{
		Parent:                  parent,
		HyperparameterTuningJob: fx.Create(parent),
	}))
	created := createResp.Msg
	assert.NilError(t, err)
	return created
}

type JobServiceModelDeploymentMonitoringJobTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() JobServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *aiplatformpb.ModelDeploymentMonitoringJob
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *aiplatformpb.ModelDeploymentMonitoringJob
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
			Parent:                       "",
			ModelDeploymentMonitoringJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
			Parent:                       "invalid resource name",
			ModelDeploymentMonitoringJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		createResp, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
			Parent:                       parent,
			ModelDeploymentMonitoringJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		assert.Check(t, msg.CreateTime != nil)
		assert.Check(t, !msg.CreateTime.AsTime().IsZero())
		assert.Check(t, msg.CreateTime.AsTime().After(beforeCreate), "msg.CreateTime (%v) is not after beforeCreate (%v)", msg.CreateTime.AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		createResp, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
			Parent:                       parent,
			ModelDeploymentMonitoringJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		getResp, err := fx.Service().GetModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetModelDeploymentMonitoringJobRequest{
			Name: msg.Name,
		}))
		persisted := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".endpoint", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("endpoint")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".model_deployment_monitoring_objective_configs", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("model_deployment_monitoring_objective_configs")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".model_deployment_monitoring_schedule_config", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("model_deployment_monitoring_schedule_config")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".model_deployment_monitoring_schedule_config.monitor_interval", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetModelDeploymentMonitoringScheduleConfig()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("monitor_interval")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".logging_sampling_strategy", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("logging_sampling_strategy")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".stats_anomalies_base_directory.output_uri_prefix", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetStatsAnomaliesBaseDirectory()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("output_uri_prefix")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetEncryptionSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".endpoint", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			container.Endpoint = "invalid resource name"
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".model_monitoring_alert_config.notification_channels", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetModelMonitoringAlertConfig()
			if container == nil {
				t.Skip("not reachable")
			}
			container.NotificationChannels = []string{"invalid resource name"}
			_, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
				Parent:                       parent,
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetModelDeploymentMonitoringJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetModelDeploymentMonitoringJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetModelDeploymentMonitoringJobRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetModelDeploymentMonitoringJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetModelDeploymentMonitoringJobRequest{
			Name: "projects/-/locations/-/modelDeploymentMonitoringJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.Name = ""
		_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
			ModelDeploymentMonitoringJob: msg,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.Name = "invalid resource name"
		_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
			ModelDeploymentMonitoringJob: msg,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.Name = created.Name + "notfound"
			_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
				ModelDeploymentMonitoringJob: msg,
			}))
			assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
				ModelDeploymentMonitoringJob: created,
				UpdateMask: &fieldmaskpb.FieldMask{
					Paths: []string{
						"invalid_field_xyz",
					},
				},
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".display_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.ModelDeploymentMonitoringJob)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
					ModelDeploymentMonitoringJob: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".endpoint", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.ModelDeploymentMonitoringJob)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("endpoint")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
					ModelDeploymentMonitoringJob: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".model_deployment_monitoring_objective_configs", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.ModelDeploymentMonitoringJob)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("model_deployment_monitoring_objective_configs")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
					ModelDeploymentMonitoringJob: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".model_deployment_monitoring_schedule_config", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.ModelDeploymentMonitoringJob)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("model_deployment_monitoring_schedule_config")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
					ModelDeploymentMonitoringJob: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".model_deployment_monitoring_schedule_config.monitor_interval", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.ModelDeploymentMonitoringJob)
				container := msg.GetModelDeploymentMonitoringScheduleConfig()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("monitor_interval")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
					ModelDeploymentMonitoringJob: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".logging_sampling_strategy", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.ModelDeploymentMonitoringJob)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("logging_sampling_strategy")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
					ModelDeploymentMonitoringJob: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".stats_anomalies_base_directory.output_uri_prefix", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.ModelDeploymentMonitoringJob)
				container := msg.GetStatsAnomaliesBaseDirectory()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("output_uri_prefix")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
					ModelDeploymentMonitoringJob: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
			t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*aiplatformpb.ModelDeploymentMonitoringJob)
				container := msg.GetEncryptionSpec()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
				container.ProtoReflect().Clear(fd)
				_, err := fx.Service().UpdateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.UpdateModelDeploymentMonitoringJobRequest{
					ModelDeploymentMonitoringJob: msg,
					UpdateMask: &fieldmaskpb.FieldMask{
						Paths: []string{
							"*",
						},
					},
				}))
				assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
			})
		})

	}
}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.ModelDeploymentMonitoringJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.ModelDeploymentMonitoringJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.ModelDeploymentMonitoringJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.ModelDeploymentMonitoringJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.ModelDeploymentMonitoringJobs))
				msgs = append(msgs, response.ModelDeploymentMonitoringJobs...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.ModelDeploymentMonitoringJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				_, err := fx.Service().DeleteModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteModelDeploymentMonitoringJobRequest{
					Name: parentMsgs[i].Name,
				}))
				assert.NilError(t, err)
			}
			listResp, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
				Parent:   parent,
				PageSize: 9999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.ModelDeploymentMonitoringJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.ModelDeploymentMonitoringJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.ModelDeploymentMonitoringJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.ModelDeploymentMonitoringJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListModelDeploymentMonitoringJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListModelDeploymentMonitoringJobsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.ModelDeploymentMonitoringJobs...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.ModelDeploymentMonitoringJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteModelDeploymentMonitoringJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteModelDeploymentMonitoringJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteModelDeploymentMonitoringJobRequest{
			Name: created.Name,
		}))
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteModelDeploymentMonitoringJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		deleteResp, err := fx.Service().DeleteModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteModelDeploymentMonitoringJobRequest{
			Name: created.Name,
		}))
		deleted := deleteResp.Msg
		assert.NilError(t, err)
		_ = deleted
		_, err = fx.Service().DeleteModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteModelDeploymentMonitoringJobRequest{
			Name: created.Name,
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteModelDeploymentMonitoringJobRequest{
			Name: "projects/-/locations/-/modelDeploymentMonitoringJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *JobServiceModelDeploymentMonitoringJobTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.ModelDeploymentMonitoringJob {
	t.Helper()
	createResp, err := fx.Service().CreateModelDeploymentMonitoringJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateModelDeploymentMonitoringJobRequest{
		Parent:                       parent,
		ModelDeploymentMonitoringJob: fx.Create(parent),
	}))
	created := createResp.Msg
	assert.NilError(t, err)
	return created
}

type JobServiceNasJobTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() JobServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *aiplatformpb.NasJob
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *JobServiceNasJobTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *JobServiceNasJobTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
			Parent: "",
			NasJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
			Parent: "invalid resource name",
			NasJob: fx.Create(fx.nextParent(t, false)),
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		createResp, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
			Parent: parent,
			NasJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		assert.Check(t, msg.CreateTime != nil)
		assert.Check(t, !msg.CreateTime.AsTime().IsZero())
		assert.Check(t, msg.CreateTime.AsTime().After(beforeCreate), "msg.CreateTime (%v) is not after beforeCreate (%v)", msg.CreateTime.AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		createResp, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
			Parent: parent,
			NasJob: fx.Create(parent),
		}))
		msg := createResp.Msg
		assert.NilError(t, err)
		getResp, err := fx.Service().GetNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasJobRequest{
			Name: msg.Name,
		}))
		persisted := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("nas_job_spec")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.metric.metric_id", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetMetric()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("metric_id")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.metric.goal", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetMetric()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("goal")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("search_trial_spec")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.search_trial_job_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("search_trial_job_spec")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.search_trial_job_spec.worker_pool_specs", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec().GetSearchTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("worker_pool_specs")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.search_trial_job_spec.base_output_directory.output_uri_prefix", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec().GetSearchTrialJobSpec().GetBaseOutputDirectory()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("output_uri_prefix")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.max_trial_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("max_trial_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.max_parallel_trial_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("max_parallel_trial_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.train_trial_spec.train_trial_job_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetTrainTrialSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("train_trial_job_spec")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.train_trial_spec.max_parallel_trial_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetTrainTrialSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("max_parallel_trial_count")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.train_trial_spec.frequency", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetTrainTrialSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("frequency")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetEncryptionSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
			container.ProtoReflect().Clear(fd)
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.search_trial_job_spec.network", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec().GetSearchTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Network = "invalid resource name"
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.search_trial_job_spec.tensorboard", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec().GetSearchTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Tensorboard = "invalid resource name"
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.search_trial_job_spec.experiment", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec().GetSearchTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Experiment = "invalid resource name"
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.search_trial_job_spec.experiment_run", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec().GetSearchTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.ExperimentRun = "invalid resource name"
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
		t.Run(".nas_job_spec.multi_trial_algorithm_spec.search_trial_spec.search_trial_job_spec.models", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetNasJobSpec().GetMultiTrialAlgorithmSpec().GetSearchTrialSpec().GetSearchTrialJobSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			container.Models = []string{"invalid resource name"}
			_, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
				Parent: parent,
				NasJob: msg,
			}))
			assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
		})
	})

}

func (fx *JobServiceNasJobTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasJobRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasJobRequest{
			Name: "projects/-/locations/-/nasJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceNasJobTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.NasJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.NasJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.NasJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.NasJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.NasJobs))
				msgs = append(msgs, response.NasJobs...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.NasJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				_, err := fx.Service().DeleteNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteNasJobRequest{
					Name: parentMsgs[i].Name,
				}))
				assert.NilError(t, err)
			}
			listResp, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
				Parent:   parent,
				PageSize: 9999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.NasJobs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.NasJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.NasJob, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.NasJob, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListNasJobs(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasJobsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.NasJobs...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.NasJob) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *JobServiceNasJobTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteNasJobRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteNasJobRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteNasJobRequest{
			Name: created.Name,
		}))
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().DeleteNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteNasJobRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		deleteResp, err := fx.Service().DeleteNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteNasJobRequest{
			Name: created.Name,
		}))
		deleted := deleteResp.Msg
		assert.NilError(t, err)
		_ = deleted
		_, err = fx.Service().DeleteNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteNasJobRequest{
			Name: created.Name,
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().DeleteNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.DeleteNasJobRequest{
			Name: "projects/-/locations/-/nasJobs/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceNasJobTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *JobServiceNasJobTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *JobServiceNasJobTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *JobServiceNasJobTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.NasJob {
	t.Helper()
	createResp, err := fx.Service().CreateNasJob(fx.Context(), connect.NewRequest(&aiplatformpb.CreateNasJobRequest{
		Parent: parent,
		NasJob: fx.Create(parent),
	}))
	created := createResp.Msg
	assert.NilError(t, err)
	return created
}

type JobServiceNasTrialDetailTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() JobServiceClient
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// CreateResource should create a NasTrialDetail and return it.
	// If the field is not set, some tests will be skipped.
	//
	// This method is generated because service does not expose a Create
	// method (or it does not comply with AIP).
	CreateResource func(ctx context.Context, parent string) (*aiplatformpb.NasTrialDetail, error)
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *JobServiceNasTrialDetailTestSuiteConfig) test(t *testing.T) {
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
}

func (fx *JobServiceNasTrialDetailTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetNasTrialDetail(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasTrialDetailRequest{
			Name: "",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetNasTrialDetail(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasTrialDetailRequest{
			Name: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		getResp, err := fx.Service().GetNasTrialDetail(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasTrialDetailRequest{
			Name: created.Name,
		}))
		msg := getResp.Msg
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		_, err := fx.Service().GetNasTrialDetail(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasTrialDetailRequest{
			Name: created.Name + "notfound",
		}))
		assert.Equal(t, connect.CodeNotFound, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().GetNasTrialDetail(fx.Context(), connect.NewRequest(&aiplatformpb.GetNasTrialDetailRequest{
			Name: "projects/-/locations/-/nasJobs/-/nasTrialDetails/-",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

}

func (fx *JobServiceNasTrialDetailTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		_, err := fx.Service().ListNasTrialDetails(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasTrialDetailsRequest{
			Parent: "invalid resource name",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListNasTrialDetails(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasTrialDetailsRequest{
			Parent:    parent,
			PageToken: "invalid page token",
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		_, err := fx.Service().ListNasTrialDetails(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasTrialDetailsRequest{
			Parent:   parent,
			PageSize: -10,
		}))
		assert.Equal(t, connect.CodeInvalidArgument, connect.CodeOf(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.NasTrialDetail, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListNasTrialDetails(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasTrialDetailsRequest{
				Parent:   parent,
				PageSize: 999,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.NasTrialDetails,
				cmpopts.SortSlices(func(a, b *aiplatformpb.NasTrialDetail) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListNasTrialDetails(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasTrialDetailsRequest{
				Parent:   parent,
				PageSize: resourcesCount,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Equal(t, "", response.NextPageToken)
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			listResp, err := fx.Service().ListNasTrialDetails(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasTrialDetailsRequest{
				Parent:   parent,
				PageSize: resourcesCount - 1,
			}))
			response := listResp.Msg
			assert.NilError(t, err)
			assert.Check(t, response.NextPageToken != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.NasTrialDetail, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListNasTrialDetails(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasTrialDetailsRequest{
					Parent:    parent,
					PageSize:  1,
					PageToken: nextPageToken,
				}))
				response := listResp.Msg
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.NasTrialDetails))
				msgs = append(msgs, response.NasTrialDetails...)
				nextPageToken = response.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.NasTrialDetail) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*aiplatformpb.NasTrialDetail, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*aiplatformpb.NasTrialDetail, 0, resourcesCount)
			var nextPageToken string
			for {
				listResp, err := fx.Service().ListNasTrialDetails(fx.Context(), connect.NewRequest(&aiplatformpb.ListNasTrialDetailsRequest{
					Parent:    parent,
					PageSize:  0,
					PageToken: nextPageToken,
				}))
				page := listResp.Msg
				assert.NilError(t, err)
				msgs = append(msgs, page.NasTrialDetails...)
				nextPageToken = page.NextPageToken
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *aiplatformpb.NasTrialDetail) bool {
					return a.Name < b.Name
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *JobServiceNasTrialDetailTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *JobServiceNasTrialDetailTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *JobServiceNasTrialDetailTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *JobServiceNasTrialDetailTestSuiteConfig) create(t *testing.T, parent string) *aiplatformpb.NasTrialDetail {
	t.Helper()
	if fx.CreateResource == nil {
		t.Skip("Test skipped because CreateResource not specified on JobServiceNasTrialDetailTestSuiteConfig")
	}
	created, err := fx.CreateResource(fx.Context(), parent)
	assert.NilError(t, err)
	return created
}
